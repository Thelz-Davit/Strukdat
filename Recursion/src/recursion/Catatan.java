/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package recursion;

/**
 *
 * @author Davit
 */
public class Catatan {
    /*
        O(1) = constant (akses array, assign var, if)
        O(n) = Linear(iterasi array, loop)
        O(log n) = logarithmic (menemukan elemen sorted array, loopnya param nya dibagi dua)
        O(n^2) = quadratic (melihat index di dalam array dua kali, loop bersarang)
        O(2^n) = Exponensial (Double rekursi dalam fibonaci)
    
    */
}
